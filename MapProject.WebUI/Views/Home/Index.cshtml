@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/UI/Index.cshtml";
}

<!-- Hero Start -->
<div class="container-fluid bg-primary py-5 mb-5 hero-header">
    <div class="container py-5">
        <div class="row justify-content-start">
            <div class="col-lg-8 text-start">
                <h1 class="text-white mb-md-4" style="font-size: clamp(1.8rem, 7vw, 5rem); font-weight: 700; line-height: 1.15;">Haritaya dair her şey..</h1>

                <div class="pt-2 d-flex flex-row flex-wrap gap-3 justify-content-start align-items-center">
                </div>
            </div>
        </div>
    </div>
</div>


@await Component.InvokeAsync("HomeAbout")

@await Component.InvokeAsync("CoureselSection")

@await Component.InvokeAsync("HomeVideoSection")

@await Component.InvokeAsync("HomeFavoriteMaps")

@await Component.InvokeAsync("HomeContact")

@await Component.InvokeAsync("HomeCategoryList")

